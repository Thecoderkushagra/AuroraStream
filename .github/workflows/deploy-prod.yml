name: Deploy to Prod

on:
  push:
    branches:
      - main
  release:
    types: [published]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven (Parent POM)
        run: mvn clean package -DskipTests

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Configure Docker to use GAR
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      # Placeholder: Build and push each microservice image
      - name: Build and Push Docker Images
        run: |
          echo "Building and pushing docker images to Google Artifact Registry..."
          # docker build -t us-central1-docker.pkg.dev/$PROJECT_ID/aurora-repo/user-service:prod ./user-service
          # docker push us-central1-docker.pkg.dev/$PROJECT_ID/aurora-repo/user-service:prod
